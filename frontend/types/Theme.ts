export type Theme = {
  text: string;
  blue: string;
  green: string;
  red: string;
  white: string;
  border: string;
  purple: string;
  darkGray: string;
  lightGray: string;
  background: string;
  tabIconDefault: string;
  tabIconSelected: string;
};

export type ThemeType = {
  light: Theme;
  dark: Theme;
};
