import { CustomBackground, CustomText } from '@/components'
import { View, Text } from 'react-native'

export default function Index() {
  return (
    <CustomBackground>
      <CustomText>Index Savings Screen</CustomText>
    </CustomBackground>
  )
}